{
  "name": "jev-servicenow-transformers",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript source for the ServiceNow Custom LLM Transformer scripts that bridge Jev (TypeSafe System One) into the Generative AI Controller.",
  "scripts": {
    "build": "tsc",
    "typecheck:fluent": "tsc -p tsconfig.fluent.json",
    "evaluate:jev": "tsx src/evaluate-jev.ts"
  },
  "devDependencies": {
    "@servicenow/sdk": "^4.12.2",
    "tsx": "^4.23.15",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^4.0.87",
    "ai": "^7.0.107"
  }
}
