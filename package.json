{
  "name": "jev-servicenow-transformers",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript source for the ServiceNow AI Agent Studio Script tool that calls Jev (TypeSafe System One) directly over HTTP.",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@servicenow/sdk": "^4.12.2",
    "typescript": "^5.5.4"
  }
}
